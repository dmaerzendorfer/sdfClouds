
void Scene(Texture2D<float4> clouds, float cloudCount, out float d)
{
    //https://www.youtube.com/watch?v=hXYOlXVRRL8
    //check this out again, shader graph doesnt have loops nor arrays -> got textures tho and custom functions
    //maybe not worth it to do it in shader graph?

    //overall, think its smarter to do it in hlsl...
    //also got the ref of https://blog.maximeheckel.com/posts/real-time-cloudscapes-with-volumetric-raymarching/
}